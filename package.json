{
  "dependencies": {
    "@emotion/is-prop-valid": "^1.3.1"
  },
  "devDependencies": {
    "vite": "^4.5.0",
    "typescript": "^5.0.0",
    "gh-pages": "^6.0.0"
  },
  "scripts": {
    "build": "tsc && vite build",
    "deploy": "npm run build && gh-pages -d dist"
  }
}
